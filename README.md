# Bramble & Oak

Fixture site for said-n-done Phase 1 testing. Static HTML/CSS/JS, no build step.
Deployed on Vercel.

CI: html-validate on every PR. Auto-merge enabled once required checks pass.

Vercel deploys `main` on push (Git integration connected 2026-08-27); no manual
`vercel --prod` is needed.
